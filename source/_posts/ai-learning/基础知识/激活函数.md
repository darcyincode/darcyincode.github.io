---
title: 激活函数（Activation Functions）
date: 2026-02-04
categories:
  - [大模型, 数学基础]
tags:
  - 深度学习
  - 神经网络
  - AI
---

# 激活函数（Activation Functions）

> **📊 图形渲染说明：** 本文档使用 LaTeX/TikZ 绘制专业数学函数图形
> 
> **快速开始（VS Code）：**
> 1. 安装插件：`Markdown Preview Enhanced`
> 2. 按 `Ctrl+Shift+P`，输入 `Markdown Preview Enhanced: Extend Parser`
> 3. 在配置文件中确保 `enableExtendedTableSyntax: true`
> 4. 右键文档 → "Markdown Preview Enhanced: Open Preview to the Side"
> 5. 首次渲染可能需要下载LaTeX包，请等待
> 
> **如果图形无法显示：**
> - 检查是否安装了LaTeX（MiKTeX/TeXLive）
> - 或使用在线查看器：将代码复制到 [Overleaf](https://www.overleaf.com/)
> - 或查看生成的图片版本（如果有）
> 
> **图片说明：** 所有灰色框内的代码块都是可渲染的TikZ图形

## 什么是激活函数？

激活函数是神经网络中的**非线性变换函数**，应用于神经元的加权和之后，决定神经元是否被"激活"以及激活的程度。

**计算流程：**
```
输入 → 加权求和 → 激活函数 → 输出
 x   →  z = wx+b  →  a = f(z)  →  a
```

**数学表达：**
$$z = \mathbf{w}^T \mathbf{x} + b$$
$$a = f(z)$$

其中 $f$ 就是激活函数。

## 为什么需要激活函数？

### 1. 引入非线性

**没有激活函数的问题：**

假设有一个两层网络，每层都是线性变换：
$$h = W_1 x + b_1$$
$$y = W_2 h + b_2$$

代入得：
$$y = W_2(W_1 x + b_1) + b_2 = W_2 W_1 x + W_2 b_1 + b_2$$

令 $W = W_2 W_1$，$b = W_2 b_1 + b_2$，则：
$$y = Wx + b$$

**结论：** 多层线性变换仍然是线性的，深度网络退化为单层！

### 2. 解决实际问题的能力

- **线性模型只能解决线性可分问题**（如简单的直线分类）
- **非线性激活函数使网络能学习复杂的决策边界**（如XOR问题、图像识别）

### 3. 通用逼近定理

带有非线性激活函数的神经网络可以逼近任何连续函数（在一定条件下）。

**简单理解：**

想象你要画一条复杂的曲线：
- **线性函数**：只能画直线，无论组合多少层
- **非线性激活函数**：可以通过调整参数，画出任意复杂的曲线

**理论保证：**
只要有足够多的隐藏层神经元，带有非线性激活函数的神经网络理论上可以拟合：
- 任意复杂的图像识别边界
- 任意非线性的语言模式
- 任意连续的数据关系

**实际例子：**
```
简单函数: y = 2x + 1        → 线性网络就能学
复杂函数: y = sin(x²) + log(x) → 需要非线性激活函数
图像分类: 猫 vs 狗            → 需要非线性激活函数
```

**核心条件：**
- ✅ 必须有非线性激活函数
- ✅ 需要足够的神经元（网络容量）
- ✅ 有效的训练方法

这就是为什么激活函数是深度学习的"灵魂"！

## 激活函数的理想特性

✅ **非线性**：引入非线性变换能力  
✅ **可微分**：便于反向传播计算梯度  
✅ **单调性**：保持优化的凸性（可选）  
✅ **输出范围有界**：某些情况下有助于稳定训练  
✅ **计算高效**：减少计算开销  
✅ **避免梯度消失**：深层网络训练的关键  
✅ **零中心化**：加速收敛（可选）

## 常用激活函数详解

### 1. Sigmoid（S型函数）

#### 公式
$$\sigma(z) = \frac{1}{1 + e^{-z}}$$

#### 导数
$$\sigma'(z) = \sigma(z)(1 - \sigma(z))$$

#### 特性
- **输出范围**：(0, 1)
- **形状**：S型曲线
- **可解释性**：可视为概率

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\sigma(z)$},
    title={Sigmoid激活函数},
    grid=major,
    domain=-6:6,
    samples=100,
    ymin=-0.1, ymax=1.1,
    xmin=-6, xmax=6,
    legend pos=north west,
    axis lines=middle,
]
\addplot[blue, thick, smooth] {1/(1+exp(-x))};
\addlegendentry{$\sigma(z) = \frac{1}{1+e^{-z}}$}

% 标注关键点
\addplot[mark=*, red] coordinates {(0,0.5)};
\node[red, above right] at (axis cs:0,0.5) {拐点 (0, 0.5)};

% 饱和区域标注
\draw[dashed, gray] (axis cs:-6,0) -- (axis cs:6,0);
\draw[dashed, gray] (axis cs:-6,1) -- (axis cs:6,1);
\node[gray] at (axis cs:-4,0.9) {饱和区};
\node[gray] at (axis cs:0,0.7) {过渡区};
\node[gray] at (axis cs:4,0.9) {饱和区};
\end{axis}
\end{tikzpicture}
```

**关键点：**
- 在 z=0 时，σ(0) = 0.5
- z > 2 时输出接近1（上饱和）
- z < -2 时输出接近0（下饱和）

#### 优点
✅ 输出在(0,1)，适合二分类输出层  
✅ 平滑可微  
✅ 有明确函数曲线

**ASCII可视化：**
```
   1.0 │        ┌────────────
       │      ╱
   0.5 │    ╱
       │   ╱
   0.0 ┼──╱─────────────────
       │ ╱
  -0.5 │╱
       │
  -1.0 │────────╯
       └────────────────────
      -4  -2   0   2   4
```

- ❌ 隐藏层（已基本被ReLU取代）

---

### 2. Tanh（双曲正切）

#### 公式
$$\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}} = \frac{e^{2z} - 1}{e^{2z} + 1}$$

或表示为：
$$\tanh(z) = 2\sigma(2z) - 1$$

#### 导数
$$\tanh'(z) = 1 - \tanh^2(z)$$

#### 特性
- **输出范围**：(-1, 1)
- **零中心化**：是的
- **形状**：S型，比Sigmoid陡峭

#### 函数曲线

```latex
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\tanh(z)$},
    title={Tanh激活函数},
    grid=major,
    domain=-4:4,
    samples=100,
    ymin=-1.2, ymax=1.2,
    xmin=-4, xmax=4,
    legend pos=north west,
    axis lines=middle,
]
\addplot[red, thick, smooth] {tanh(x)};
\addlegendentry{$\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}}$}

% 标注零中心点
\addplot[mark=*, blue] coordinates {(0,0)};
\node[blue, above right] at (axis cs:0,0) {零中心 (0, 0)};

% 饱和区域标注
\draw[dashed, gray] (axis cs:-4,-1) -- (axis cs:4,-1);
\draw[dashed, gray] (axis cs:-4,1) -- (axis cs:4,1);
\node[gray] at (axis cs:-2.5,0.85) {饱和区};
\node[gray] at (axis cs:2.5,0.85) {饱和区};
\end{axis}
\end{tikzpicture}
```

**关键点：**
- 在 z=0 时，tanh(0) = 0（零中心化）
- 输出范围：(-1, 1)
- 比Sigmoid更陡峭

#### 优点
✅ **零中心化**：比Sigmoid好  
✅ 输出范围对称  
✅ 收敛速度比Sigmoid快
函数曲线

**ASCII可视化：**
```
   3.0 │           ╱
       │         ╱
   2.0 │       ╱
       │     ╱
   1.0 │   ╱
       │ ╱
   0.0 ┼───────────────────
       │
  -1.0 │
       └────────────────────
      -3  -2  -1  0  1  2  3
```

**Python绘图代码：**
```python
#### 导数
$$\text{ReLU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ 0 & \text{if } z \leq 0 \end{cases}$$

（$z=0$ 处导数未定义，实践中设为0或1）

#### 特性
- **输出范围**：[0, +∞)
- **线性区域**：正半轴为线性
- **稀疏激活**：约50%的神经元被激活

#### 函数曲线

```
  ReLU(z)
   3.0 ┤                    ╱
       │                  ╱
   2.5 ┤                ╱
       │              ╱
   2.0 ┤            ╱
       │          ╱
   1.5 ┤        ╱
       │      ╱
   1.0 ┤    ╱
       │  ╱
   0.5 ┤╱
       ╱
   0.0 ┼──────────────────────────────
       │
  -0.5 │
       │
  -1.0 │
       └──────────────────────────────
      -3  -2  -1   0   1   2   3    z
       
      死区   ← 转折点 →  线性区
     (梯度=0)  (z=0)    (梯度=1)
```

**关键特征：**
- z ≤ 0：输出为0，梯度为0（可能导致神经元死亡）
- z ≤ 0：输出为0，梯度为0（可能导致神经元死亡）
- z > 0：输出=z，梯度恒为1（无梯度消失）
- 在 z=0 处有一个"拐点"

#### 优点
✅ **计算简单**：只需比较和取最大值  
✅ **缓解梯度消失**：正区域梯度恒为1  
✅ **稀疏激活**：部分神经元输出0，减少计算  
✅ **收敛速度快**：比Sigmoid/Tanh快6倍（Krizhevsky等，2012）

#### 缺点
❌ **神经元死亡（Dying ReLU）**：负区域梯度为0，一旦进入负区域可能永远无法恢复  
❌ **非零中心化**：输出恒为正  
❌ **无界输出**：可能导致激活值爆炸

#### 适用场景
- ✅ **深度前馈网络的隐藏层**（最常用）
- ✅ CNN的卷积层
- ⚠️ 需要配合合理的学习率和初始化

---

### 4. Leaky ReLU

#### 公式
$$\text{Leaky ReLU}(z) = \max(\alpha z, z) = \begin{cases} z & \text{if } z > 0 \\ \alpha z & \text{if } z \leq 0 \end{cases}$$

通常 $\alpha = 0.01$

#### 导数
$$\text{Leaky ReLU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ \alpha & \text{if } z \leq 0 \end{cases}$$

#### 函数曲线

```latex
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\text{Leaky ReLU}(z)$},
    title={Leaky ReLU激活函数 ($\alpha=0.01$)},
    grid=major,
    domain=-3:3,
    samples=100,
    ymin=-0.1, ymax=3.5,
    xmin=-3, xmax=3,
    legend pos=north west,
    axis lines=middle,
]
\addplot[magenta, thick, smooth] {max(0.01*x,x)};
\addlegendentry{$\text{Leaky ReLU}(z)$}

% 标注转折点
\addplot[mark=*, red] coordinates {(0,0)};
\node[red, above right] at (axis cs:0,0) {转折点 (0, 0)};

% 区域标注
\node[gray] at (axis cs:-2,1.5) {小梯度区};
\node[gray] at (axis cs:-2,1) {(梯度=0.01)};
\node[gray] at (axis cs:2,2.5) {线性区};
\node[gray] at (axis cs:2,2) {(梯度=1)};

% 梯度区域着色
\fill[orange!10] (axis cs:-3,-0.1) rectangle (axis cs:0,0);
\fill[green!10] (axis cs:0,0) rectangle (axis cs:3,3);

% 对比虚线显示ReLU
\addplot[gray, dashed, thin] {max(0,x)};
\addlegendentry{ReLU (对比)};
\end{axis}
\end{tikzpicture}
```

**关键改进：**
- z ≤ 0：输出 = αz，小梯度α（通常0.01）
- z > 0：输出 = z，梯度为1
- **解决了Dying ReLU问题**
- ⚠️ 需要配合合理的学习率和初始化

---

### 4. Leaky ReLU

#### 公式
$$\text{Leaky ReLU}(z) = \max(\alpha z, z) = \begin{cases} z & \text{if } z > 0 \\ \alpha z & \text{if } z \leq 0 \end{cases}$$

通常 $\alpha = 0.01$

#### 导数
$$\text{Leaky ReLU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ \alpha & \text{if } z \leq 0 \end{cases}$$

#### 图形
```
    │      ╱
  2 │    ╱
    │  ╱
  1 │╱
    ╱────────
  0 └──────────
   -2  0  2
```

#### 优点
✅ **解决Dying ReLU问题**：负区域有小梯度  
✅ 保留ReLU的大部分优点

#### 缺点
❌ 需要调整超参数 $\alpha$  
❌ 效果提升有限

#### 变体
- **PReLU（Parametric ReLU）**：$\alpha$ 作为可学习参数
- **RReLU（Randomized ReLU）**：训练时 $\alpha$ 随机，测试时固定

---

### 5. ELU（指数线性单元）

#### 公式
$$\text{ELU}(z) = \begin{cases} z & \text{if } z > 0 \\ \alpha(e^z - 1) & \text{if } z \leq 0 \end{cases}$$

通常 $\alpha = 1$

#### 导数
$$\text{ELU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ \alpha e^z & \text{if } z \leq 0 \end{cases}$$

#### 优点
✅ **接近零中心化**：负值输出减少偏移  
✅ **平滑性好**：负区域指数曲线  
✅ 缓解梯度消失

#### 缺点
❌ 计算指数函数开销大

---

### 6. GELU（高斯误差线性单元）

#### 公式
$$\text{GELU}(z) = z \cdot \Phi(z)$$

其中 $\Phi(z)$ 是标准正态分布的累积分布函数。

**近似公式：**
$$\text{GELU}(z) \approx 0.5z\left(1 + \tanh\left[\sqrt{\frac{2}{\pi}}(z + 0.044715z^3)\right]\right)$$

#### 特性
- **非单调**：在负区域有轻微下降
- **平滑可微**
- **随机正则化性质**

#### 优点
✅ **Transformer的标准选择**（BERT、GPT等）  
✅ 性能优于ReLU在某些任务上

#### 缺点
❌ 计算复杂度高

---

### 7. Swish（自门控激活）

#### 公式
$$\text{Swish}(z) = z \cdot \sigma(z) = \frac{z}{1 + e^{-z}}$$

#### 特性
- **平滑非单调**
- **自门控**：输出由输入本身调节
- 由Google提出（Ramachandran等，2017）

#### 优点
✅ 在深度网络中性能优于ReLU  
✅ 搜索得到的最优激活函数之一

#### 缺点
❌ 计算开销比ReLU大

---

### 8. Softmax（归一化指数）

#### 公式
对于向量 $\mathbf{z} = [z_1, z_2, ..., z_K]$：

$$\text{softmax}(z_i) = \frac{e^{z_i}}{\sum_{j=1}^{K} e^{z_j}}$$

#### 特性
- **输出**：概率分布（和为1）
- **多分类**：K个类别
- **强竞争性**：突出最大值

#### 性质
- $\sum_{i=1}^{K} \text{softmax}(z_i) = 1$
- $0 < \text{softmax}(z_i) < 1$

#### 适用场景
- ✅ **多分类问题的输出层**（必用）
- 配合交叉熵损失函数

#### 数值稳定技巧
为防止指数溢出：
$$\text{softmax}(z_i) = \frac{e^{z_i - \max(\mathbf{z})}}{\sum_{j=1}^{K} e^{z_j - \max(\mathbf{z})}}$$

---

## 激活函数对比总结

### 综合对比表

| 激活函数 | 输出范围 | 零中心 | 梯度消失 | 计算复杂度 | 主要用途 |
|---------|---------|--------|---------|-----------|---------|
| Sigmoid | (0, 1) | ❌ | 严重 | 高 | 二分类输出 |
| Tanh | (-1, 1) | ✅ | 严重 | 高 | RNN隐藏层 |
| ReLU | [0, +∞) | ❌ | 轻微 | 低 | **CNN/FNN隐藏层** |
| Leaky ReLU | (-∞, +∞) | ❌ | 很小 | 低 | 深度网络 |
| ELU | (-α, +∞) | 近似 | 很小 | 中 | 深度网络 |
| GELU | (-∞, +∞) | ✅ | 很小 | 高 | **Transformer** |
| Swish | (-∞, +∞) | ❌ | 很小 | 中 | 深度网络 |
| Softmax | (0, 1) | ❌ | - | 中 | **多分类输出** |

### 梯度对比

| 函数 | 导数表达式 | 最大梯度 | 梯度消失风险 |
|------|-----------|---------|-------------|
| Sigmoid | $\sigma(1-\sigma)$ | 0.25 | 高 |
| Tanh | $1-\tanh^2$ | 1 | 中 |
| ReLU | 0 或 1 | 1 | 低 |
| Leaky ReLU | α 或 1 | 1 | 极低 |

---

## 如何选择激活函数？

### 按网络层次选择

#### 隐藏层
1. **默认选择：ReLU**
   - 适用于大多数深度网络
   - 训练快速，效果好

2. **ReLU效果不佳时：**
   - 尝试 **Leaky ReLU** 或 **ELU**
   - Transformer架构：**GELU**

3. **RNN/LSTM：**
   - 使用 **Tanh**（标准配置）

#### 输出层
- **二分类**：Sigmoid
- **多分类**：Softmax
- **回归**：线性激活（恒等函数）
- **多标签分类**：Sigmoid（每个输出独立）

### 按任务类型选择

| 任务类型 | 隐藏层 | 输出层 |
|---------|--------|--------|
| 图像分类（CNN） | ReLU | Softmax |
| 文本分类（Transformer） | GELU | Softmax |
| 序列建模（RNN） | Tanh | 根据任务 |
| 回归 | ReLU | 线性 |
| 生成模型（GAN） | Leaky ReLU | Tanh |

### 实验建议

1. **从ReLU开始**（99%的情况）
2. **遇到问题时尝试：**
   - Dying ReLU → Leaky ReLU
   - 需要零中心化 → ELU
   - 追求极致性能 → GELU/Swish

---

## 激活函数的数学性质
可视化工具推荐

### 在线绘图工具
- **Desmos**: https://www.desmos.com/calculator （交互式函数绘图）
- **GeoGebra**: https://www.geogebra.org/graphing （数学可视化）
- **Plotly**: 在Jupyter Notebook中交互式绘图

### 本地绘图示例（完整版）

```python
import numpy as np
import matplotlib.pyplot as plt

# 创建数据
z = np.linspace(-6, 6, 200)

# 定义激活函数
sigmoid = 1 / (1 + np.exp(-z))
tanh = np.tanh(z)
relu = np.maximum(0, z)
leaky_relu = np.where(z > 0, z, 0.01 * z)
elu = np.where(z > 0, z, 1.0 * (np.exp(z) - 1))

# 绘制所有激活函数
fig, axes = plt.subplots(2, 3, figsize=(15, 10))
fig.suptitle('常用激活函数对比', fontsize=16)

# Sigmoid
axes[0, 0].plot(z, sigmoid, 'b-', linewidth=2)
axes[0, 0].grid(True, alpha=0.3)
axes[0, 0].set_title('Sigmoid')
axes[0, 0].set_xlabel('z')
axes[0, 0].set_ylabel('σ(z)')
axes[0, 0].axhline(y=0, color='k', linestyle='--', alpha=0.3)
axes[0, 0].axvline(x=0, color='k', linestyle='--', alpha=0.3)

# Tanh
axes[0, 1].plot(z, tanh, 'r-', linewidth=2)
axes[0, 1].grid(True, alpha=0.3)
axes[0, 1].set_title('Tanh')
axes[0, 1].set_xlabel('z')
axes[0, 1].set_ylabel('tanh(z)')
axes[0, 1].axhline(y=0, color='k', linestyle='--', alpha=0.3)
axes[0, 1].axvline(x=0, color='k', linestyle='--', alpha=0.3)

# ReLU
axes[0, 2].plot(z, relu, 'g-', linewidth=2)
axes[0, 2].grid(True, alpha=0.3)
axes[0, 2].set_title('ReLU')
axes[0, 2].set_xlabel('z')
axes[0, 2].set_ylabel('ReLU(z)')
axes[0, 2].axhline(y=0, color='k', linestyle='--', alpha=0.3)
axes[0, 2].axvline(x=0, color='k', linestyle='--', alpha=0.3)

# Leaky ReLU
axes[1, 0].plot(z, leaky_relu, 'm-', linewidth=2)
axes[1, 0].grid(True, alpha=0.3)
axes[1, 0].set_title('Leaky ReLU')
axes[1, 0].set_xlabel('z')
axes[1, 0].set_ylabel('Leaky ReLU(z)')
axes[1, 0].axhline(y=0, color='k', linestyle='--', alpha=0.3)
axes[1, 0].axvline(x=0, color='k', linestyle='--', alpha=0.3)

# ELU
axes[1, 1].plot(z, elu, 'c-', linewidth=2)
axes[1, 1].grid(True, alpha=0.3)
axes[1, 1].set_title('ELU')
axes[1, 1].set_xlabel('z')
axes[1, 1].set_ylabel('ELU(z)')
axes[1, 1].axhline(y=0, color='k', linestyle='--', alpha=0.3)
axes[1, 1].axvline(x=0, color='k', linestyle='--', alpha=0.3)

# 对比图
axes[1, 2].plot(z, sigmoid, 'b-', linewidth=2, label='Sigmoid', alpha=0.7)
axes[1, 2].plot(z, tanh, 'r-', linewidth=2, label='Tanh', alpha=0.7)
axes[1, 2].plot(z, relu, 'g-', linewidth=2, label='ReLU', alpha=0.7)
axes[1, 2].grid(True, alpha=0.3)
axes[1, 2].set_title('对比')
axes[1, 2].set_xlabel('z')
axes[1, 2].set_ylabel('f(z)')
axes[1, 2].legend()
axes[1, 2].axhline(y=0, color='k', linestyle='--', alpha=0.3)
axes[1, 2].axvline(x=0, color='k', linestyle='--', alpha=0.3)

plt.tight_layout()
plt.savefig('activation_functions.png', dpi=300, bbox_inches='tight')
plt.show()
```

---

## 
### 1. 饱和性（Saturation）

**定义：** 当 $|z| \to \infty$ 时，$|f'(z)| \to 0$

- **饱和函数**：Sigmoid, Tanh（两侧饱和）
- **非饱和函数**：ReLU（正区域）

**影响：** 饱和导致梯度消失

### 2. 单调性（Monotonicity）

- **严格单调**：Sigmoid, Tanh, ReLU
- **非单调**：Swish, GELU

**影响：** 单调性有助于优化（单一方向）

### 3. 有界性（Boundedness）

- **有界**：Sigmoid (0,1), Tanh (-1,1)
- **单侧无界**：ReLU [0,∞)
- **双侧无界**：Leaky ReLU

### 4. 连续性与可微性

- **处处可微**：Sigmoid, Tanh, GELU
- **分段可微**：ReLU（$z=0$处不可微）

---

## 梯度消失与爆炸

### 梯度消失原因

在反向传播中：
$$\frac{\partial L}{\partial w_1} = \frac{\partial L}{\partial a_n} \cdot \prod_{i=1}^{n} f'(z_i) \cdot \frac{\partial z_i}{\partial w_1}$$

如果 $f'(z_i) < 1$（如Sigmoid），连乘导致梯度指数衰减。

### 解决方案

1. **使用ReLU系列**：正区域梯度恒为1
2. **批归一化（Batch Normalization）**：稳定激活值分布
3. **残差连接（Residual Connection）**：梯度直通路径
4. **梯度裁剪**：限制梯度最大值
5. **更好的初始化**：Xavier/He初始化

---

## 激活函数的历史演进

```
时间线：
1943: Threshold（阶跃函数）
  ↓
1960s: Sigmoid
  ↓
1990s: Tanh
  ↓
2000: Softmax（多分类）
  ↓
2010: ReLU（深度学习突破）
  ↓
2015: Leaky ReLU, PReLU, ELU
  ↓
2017: Swish（神经架构搜索）
  ↓
2018: GELU（Transformer时代）
  ↓
2020+: 各种优化变体（Mish, etc.）
```

---

## 代码实现示例

### NumPy实现

```python
import numpy as np

def sigmoid(z):
    return 1 / (1 + np.exp(-z))

def tanh(z):
    return np.tanh(z)

def relu(z):
    return np.maximum(0, z)

def leaky_relu(z, alpha=0.01):
    return np.where(z > 0, z, alpha * z)

def elu(z, alpha=1.0):
    return np.where(z > 0, z, alpha * (np.exp(z) - 1))

def softmax(z):
    exp_z = np.exp(z - np.max(z))  # 数值稳定
    return exp_z / exp_z.sum(axis=0)
```

### 导数实现

```python
def sigmoid_derivative(z):
    s = sigmoid(z)
    return s * (1 - s)

def tanh_derivative(z):
    return 1 - np.tanh(z)**2

def relu_derivative(z):
    return np.where(z > 0, 1, 0)

def leaky_relu_derivative(z, alpha=0.01):
    return np.where(z > 0, 1, alpha)
```

---

## 实践技巧

### 1. 初始化策略

不同激活函数需要不同的权重初始化：

- **Sigmoid/Tanh**: Xavier初始化
  $$W \sim \mathcal{N}(0, \sqrt{\frac{2}{n_{in} + n_{out}}})$$

- **ReLU**: He初始化
  $$W \sim \mathcal{N}(0, \sqrt{\frac{2}{n_{in}}})$$

### 2. 学习率调整

- ReLU可以使用较大学习率
- Sigmoid/Tanh需要较小学习率

### 3. 批归一化配合

激活函数前后使用BatchNorm可以：
- 稳定训练
- 缓解梯度消失
- 允许使用更大学习率

### 4. 调试建议

- **梯度消失**：检查激活函数导数的分布
- **梯度爆炸**：使用梯度裁剪
- **死神经元**：监控ReLU层中0输出的比例

---

## 关键要点

1. **激活函数引入非线性是深度学习的核心**
2. **ReLU是现代深度学习的默认选择**（简单高效）
3. **不同层可以使用不同激活函数**（隐藏层vs输出层）
4. **选择激活函数要考虑**：任务类型、网络深度、训练稳定性
5. **配合合适的初始化和优化器**才能发挥最佳效果
6. **理解数学性质**有助于调试和优化网络

---

## 附录：TikZ绘图完整指南

### 如何在VS Code中渲染TikZ图形

#### 方法1：Markdown Preview Enhanced（推荐）

1. **安装插件**
   ```bash
   # 在VS Code扩展商店搜索并安装
   Markdown Preview Enhanced (by Yiyi Wang)
   ```

2. **配置LaTeX引擎**
   - 按 `Ctrl+Shift+P` 打开命令面板
   - 输入 "Markdown Preview Enhanced: Extend Parser"
   - 确保启用 LaTeX 支持

3. **预览文档**
   - 右键点击 `.md` 文件
   - 选择 "Markdown Preview Enhanced: Open Preview to the Side"
   - 或快捷键 `Ctrl+K V`

#### 方法2：导出为PDF

```bash
# 安装 Pandoc 和 LaTeX
# Windows: choco install pandoc miktex
# Mac: brew install pandoc basictex
# Linux: apt-get install pandoc texlive-full

# 转换命令
pandoc 激活函数.md -o 激活函数.pdf --pdf-engine=xelatex
```

### TikZ代码模板

```latex
% 完整的独立TikZ文档模板
\documentclass[tikz,border=5pt]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}
\begin{axis}[
    width=10cm,
    height=7cm,
    xlabel={$x$},
    ylabel={$y$},
    title={函数标题},
    grid=major,
    domain=-5:5,
    samples=100,
    axis lines=middle,
    legend pos=north west,
]
% 绘制函数
\addplot[blue, thick] {x^2};
\addlegendentry{$y=x^2$}

% 添加点
\addplot[mark=*, red] coordinates {(0,0)};

% 添加注释
\node[red] at (axis cs:1,1) {关键点};
\end{axis}
\end{tikzpicture}
\end{document}
```

### 在线TikZ编辑器

如果本地无法渲染，可以使用这些在线工具：

1. **Overleaf** - https://www.overleaf.com/
   - 完整的LaTeX在线编辑器
   - 免费账户即可使用
   - 支持实时预览

2. **TikZiT** - https://tikzit.github.io/
   - 专门的TikZ图形编辑器
   - 可视化界面
   - 导出代码

3. **LaTeX.js** - https://latex.js.org/
   - 浏览器中运行LaTeX
   - 无需安装

4. **QuickLaTeX** - https://quicklatex.com/
   - 在线生成LaTeX图片
   - 可以嵌入HTML

### 常用PGFPlots函数语法

```latex
% 基本函数
\addplot {x^2};              % 二次函数
\addplot {exp(x)};           % 指数函数
\addplot {ln(x)};            % 对数函数
\addplot {sin(deg(x))};      % 三角函数
\addplot {1/(1+exp(-x))};    % Sigmoid
\addplot {tanh(x)};          % Tanh
\addplot {max(0,x)};         % ReLU

% 条件函数
\addplot {x>0 ? x : 0.01*x}; % Leaky ReLU

% 参数方程
\addplot[parametric] ({cos(t)}, {sin(t)});

% 数据点
\addplot coordinates {(0,0) (1,1) (2,4)};
```

### 自定义样式

```latex
\begin{axis}[
    % 尺寸
    width=12cm,
    height=8cm,
    
    % 坐标轴
    axis lines=middle,
    axis line style={->, >=stealth},
    
    % 标签
    xlabel={输入 $z$},
    ylabel={输出 $f(z)$},
    title={\Large\textbf{激活函数对比}},
    
    % 范围
    xmin=-5, xmax=5,
    ymin=-1, ymax=5,
    domain=-5:5,
    
    % 网格
    grid=both,
    grid style={line width=0.1pt, draw=gray!10},
    major grid style={line width=0.2pt,draw=gray!50},
    
    % 图例
    legend pos=north west,
    legend style={
        fill=white,
        fill opacity=0.8,
        draw opacity=1,
        text opacity=1,
    },
    
    % 采样
    samples=200,
]
```

---

## 扩展阅读

- **ReLU原理**: Nair & Hinton, 2010
- **Swish**: Ramachandran et al., 2017  
- **GELU**: Hendrycks & Gimpel, 2016
- **激活函数综述**: Dubey et al., 2021
- **TikZ & PGFPlots手册**: https://www.ctan.org/pkg/pgfplots
- **LaTeX数学绘图**: https://www.overleaf.com/learn/latex/Pgfplots_package
5. **配合合适的初始化和优化器**才能发挥最佳效果
6. **理解数学性质**有助于调试和优化网络

---

## 扩展阅读

- **ReLU原理**: Nair & Hinton, 2010
- **Swish**: Ramachandran et al., 2017
- **GELU**: Hendrycks & Gimpel, 2016
- **激活函数综述**: Dubey et al., 2021
