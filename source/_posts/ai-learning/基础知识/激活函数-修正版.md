---
title: 激活函数（Activation Functions）- 修正版
date: 2026-02-04
---

# 激活函数（Activation Functions）

> **📊 图形渲染说明：** 本文档使用LaTeX/TikZ绘制专业数学函数图形
> 
> **在VS Code中查看：**
> 1. 安装插件：**Markdown Preview Enhanced** 
> 2. 按 `Ctrl+K V` 打开预览，或右键选择 "Markdown Preview Enhanced: Open Preview"
> 3. 首次渲染会自动下载LaTeX包，请耐心等待（需联网）
> 
> **如果图形不显示：**
> - 确保已安装 LaTeX 发行版（推荐 [MiKTeX](https://miktex.org/) 或 [TeX Live](https://www.tug.org/texlive/)）
> - 或访问 [Overleaf](https://www.overleaf.com/) 在线查看LaTeX代码
> - 或查看本文件夹下的PNG图片版本

---

## 什么是激活函数？

激活函数是神经网络中的**非线性变换函数**，应用于神经元的加权和之后，决定神经元是否被"激活"以及激活的程度。

**计算流程：**
```
输入 → 加权求和 → 激活函数 → 输出
 x   →  z = wx+b  →  a = f(z)  →  a
```

**数学表达：**
$$z = \mathbf{w}^T \mathbf{x} + b$$
$$a = f(z)$$

其中 $f$ 就是激活函数。

---

## 为什么需要激活函数？

### 1. 引入非线性

**没有激活函数的问题：**

假设有一个两层网络，每层都是线性变换：
$$h = W_1 x + b_1$$
$$y = W_2 h + b_2$$

代入得：
$$y = W_2(W_1 x + b_1) + b_2 = W_2 W_1 x + W_2 b_1 + b_2$$

令 $W = W_2 W_1$，$b = W_2 b_1 + b_2$，则：
$$y = Wx + b$$

**结论：** 多层线性变换等价于单层线性变换！无论网络多深，都只能拟合线性函数。

### 2. 使网络具有表达能力

激活函数引入非线性后，网络可以：
- 拟合任意复杂的非线性函数（万能近似定理）
- 捕捉数据中的复杂模式
- 实现特征的层次化表示

---

## 常用激活函数

### 1. Sigmoid（逻辑函数）

#### 公式
$$\sigma(z) = \frac{1}{1 + e^{-z}}$$

#### 导数
$$\sigma'(z) = \sigma(z)(1 - \sigma(z))$$

#### 特性
- **输出范围**：(0, 1)
- **零中心化**：否（输出恒为正）
- **形状**：S型平滑曲线

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\sigma(z)$},
    title={Sigmoid激活函数},
    grid=major,
    domain=-6:6,
    samples=100,
    ymin=-0.2, ymax=1.2,
    xmin=-6, xmax=6,
    legend pos=north west,
    axis lines=middle,
]
\addplot[blue, thick, smooth] {1/(1+exp(-x))};
\addlegendentry{$\sigma(z) = \frac{1}{1 + e^{-z}}$}

% 标注关键点
\addplot[mark=*, red] coordinates {(0,0.5)};
\node[red, above left] at (axis cs:0,0.5) {中心点 (0, 0.5)};

% 标注饱和区
\draw[dashed, gray] (axis cs:-6,0) -- (axis cs:6,0);
\draw[dashed, gray] (axis cs:-6,1) -- (axis cs:6,1);
\node[gray] at (axis cs:-3.5,0.85) {上饱和区};
\node[gray] at (axis cs:-3.5,0.15) {下饱和区};

% 梯度消失区域着色
\fill[red!10, opacity=0.5] (axis cs:-6,0) rectangle (axis cs:-2,0.12);
\fill[red!10, opacity=0.5] (axis cs:2,0.88) rectangle (axis cs:6,1);
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- 当 z → +∞ 时，σ(z) → 1
- 当 z → -∞ 时，σ(z) → 0
- z = 0 时，σ(0) = 0.5
- 两端梯度接近0（梯度消失）

#### 优点
✅ 输出在(0,1)，适合二分类输出层  
✅ 平滑可微  
✅ 有明确的概率解释

#### 缺点
❌ **梯度消失**：两端梯度接近0，深层网络难以训练  
❌ **非零中心化**：输出恒为正，导致梯度更新效率低  
❌ **计算开销大**：包含指数运算

#### 适用场景
- ✅ 二分类问题的输出层
- ❌ 隐藏层（已基本被ReLU取代）

---

### 2. Tanh（双曲正切）

#### 公式
$$\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}} = \frac{e^{2z} - 1}{e^{2z} + 1}$$

或表示为：
$$\tanh(z) = 2\sigma(2z) - 1$$

#### 导数
$$\tanh'(z) = 1 - \tanh^2(z)$$

#### 特性
- **输出范围**：(-1, 1)
- **零中心化**：是
- **形状**：S型，比Sigmoid陡峭

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\tanh(z)$},
    title={Tanh激活函数},
    grid=major,
    domain=-4:4,
    samples=100,
    ymin=-1.3, ymax=1.3,
    xmin=-4, xmax=4,
    legend pos=north west,
    axis lines=middle,
]
\addplot[red, thick, smooth] {tanh(x)};
\addlegendentry{$\tanh(z) = \frac{e^z - e^{-z}}{e^z + e^{-z}}$}

% 标注零中心点
\addplot[mark=*, blue] coordinates {(0,0)};
\node[blue, above right] at (axis cs:0,0.15) {零中心 (0, 0)};

% 饱和区域标注
\draw[dashed, gray] (axis cs:-4,-1) -- (axis cs:4,-1);
\draw[dashed, gray] (axis cs:-4,1) -- (axis cs:4,1);
\node[gray] at (axis cs:-2.5,0.75) {饱和区};
\node[gray] at (axis cs:2.5,0.75) {饱和区};

% 梯度消失区域着色
\fill[red!10, opacity=0.5] (axis cs:-4,-1) rectangle (axis cs:-1.5,-0.9);
\fill[red!10, opacity=0.5] (axis cs:1.5,0.9) rectangle (axis cs:4,1);
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- 在 z=0 时，tanh(0) = 0（零中心化）
- 输出范围：(-1, 1)，比Sigmoid对称
- 比Sigmoid更陡峭，梯度更大

#### 优点
✅ **零中心化**：比Sigmoid收敛更快  
✅ 输出范围对称  
✅ 梯度比Sigmoid大

#### 缺点
❌ **仍有梯度消失问题**：两端梯度接近0  
❌ 计算开销大（包含指数运算）

#### 适用场景
- ✅ RNN/LSTM的隐藏层
- ⚠️ 前馈网络隐藏层（ReLU更常用）

---

### 3. ReLU（修正线性单元）

#### 公式
$$\text{ReLU}(z) = \max(0, z) = \begin{cases} z & \text{if } z > 0 \\ 0 & \text{if } z \leq 0 \end{cases}$$

#### 导数
$$\text{ReLU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ 0 & \text{if } z \leq 0 \end{cases}$$

（$z=0$ 处导数未定义，实践中设为0或1）

#### 特性
- **输出范围**：[0, +∞)
- **线性区域**：正半轴为线性
- **稀疏激活**：约50%的神经元被激活

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\text{ReLU}(z)$},
    title={ReLU激活函数},
    grid=major,
    domain=-3:3,
    samples=100,
    ymin=-0.5, ymax=3.5,
    xmin=-3, xmax=3,
    legend pos=north west,
    axis lines=middle,
]
\addplot[green!70!black, thick, smooth] {max(0,x)};
\addlegendentry{$\text{ReLU}(z) = \max(0, z)$}

% 标注转折点
\addplot[mark=*, red] coordinates {(0,0)};
\node[red, above right] at (axis cs:0,0.2) {转折点 (0, 0)};

% 区域标注
\node[red] at (axis cs:-1.5,2.5) {死区};
\node[red!70] at (axis cs:-1.5,2) {(梯度=0)};
\node[green!70!black] at (axis cs:1.5,2.8) {线性激活区};
\node[green!70!black!70] at (axis cs:1.5,2.3) {(梯度=1)};

% 梯度区域着色
\fill[red!10] (axis cs:-3,-0.5) rectangle (axis cs:0,0);
\fill[green!10] (axis cs:0,0) rectangle (axis cs:3,3);
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- z ≤ 0：输出为0，梯度为0（可能导致神经元死亡）
- z > 0：输出=z，梯度恒为1（无梯度消失）
- 在 z=0 处有一个"拐点"

#### 优点
✅ **计算简单**：只需比较和取最大值  
✅ **缓解梯度消失**：正区域梯度恒为1  
✅ **稀疏激活**：部分神经元输出0，减少计算  
✅ **收敛速度快**：比Sigmoid/Tanh快约6倍

#### 缺点
❌ **神经元死亡（Dying ReLU）**：负区域梯度为0，一旦进入负区域可能永远无法恢复  
❌ **非零中心化**：输出恒为正  
❌ **无界输出**：可能导致激活值爆炸

#### 适用场景
- ✅ **深度前馈网络的隐藏层**（最常用）
- ✅ CNN的卷积层
- ⚠️ 需要配合合理的学习率和初始化

---

### 4. Leaky ReLU

#### 公式
$$\text{Leaky ReLU}(z) = \max(\alpha z, z) = \begin{cases} z & \text{if } z > 0 \\ \alpha z & \text{if } z \leq 0 \end{cases}$$

通常 $\alpha = 0.01$

#### 导数
$$\text{Leaky ReLU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ \alpha & \text{if } z \leq 0 \end{cases}$$

#### 特性
- **输出范围**：(-∞, +∞)
- **负区域斜率**：α（小但非零）
- **改进点**：解决Dying ReLU问题

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$f(z)$},
    title={Leaky ReLU激活函数 ($\alpha=0.1$)},
    grid=major,
    domain=-3:3,
    samples=100,
    ymin=-0.5, ymax=3.5,
    xmin=-3, xmax=3,
    legend pos=north west,
    axis lines=middle,
]
% Leaky ReLU
\addplot[blue, thick, smooth] {max(0.1*x,x)};
\addlegendentry{Leaky ReLU ($\alpha=0.1$)}

% ReLU对比
\addplot[gray, dashed] {max(0,x)};
\addlegendentry{ReLU}

% 标注转折点
\addplot[mark=*, red] coordinates {(0,0)};
\node[red, above right] at (axis cs:0,0.15) {(0, 0)};

% 负区域斜率标注
\draw[<->, purple] (axis cs:-2,-0.2) -- (axis cs:-1,-0.1);
\node[purple] at (axis cs:-1.5,-0.4) {斜率=$\alpha$};

% 正区域斜率标注
\draw[<->, purple] (axis cs:1,1) -- (axis cs:2,2);
\node[purple] at (axis cs:1.5,1.8) {斜率=1};
\end{axis}
\end{tikzpicture}
```

**关键改进：**
- 负区域保留小梯度α，避免神经元完全死亡
- 保留ReLU的优点（正区域线性）

#### 优点
✅ **缓解Dying ReLU问题**：负区域仍有梯度  
✅ 保留ReLU的计算优势  
✅ 收敛速度快

#### 缺点
❌ 超参数α需要调整  
❌ 仍非零中心化

#### 适用场景
- ✅ 替代ReLU，特别是出现神经元死亡时

---

### 5. ELU（指数线性单元）

#### 公式
$$\text{ELU}(z) = \begin{cases} z & \text{if } z > 0 \\ \alpha(e^z - 1) & \text{if } z \leq 0 \end{cases}$$

通常 $\alpha = 1.0$

#### 导数
$$\text{ELU}'(z) = \begin{cases} 1 & \text{if } z > 0 \\ \alpha e^z = \text{ELU}(z) + \alpha & \text{if } z \leq 0 \end{cases}$$

#### 特性
- **输出范围**：(-α, +∞)
- **负区域**：指数衰减，平滑
- **均值接近0**：自动归一化效果

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$\text{ELU}(z)$},
    title={ELU激活函数 ($\alpha=1.0$)},
    grid=major,
    domain=-3:3,
    samples=100,
    ymin=-1.2, ymax=3.5,
    xmin=-3, xmax=3,
    legend pos=north west,
    axis lines=middle,
]
% ELU
\addplot[orange, thick, smooth] {x>0 ? x : (exp(x)-1)};
\addlegendentry{ELU ($\alpha=1.0$)}

% ReLU对比
\addplot[gray, dashed] {max(0,x)};
\addlegendentry{ReLU}

% 标注饱和值
\draw[dashed, red] (axis cs:-3,-1) -- (axis cs:0,-1);
\node[red] at (axis cs:-1.5,-1.3) {饱和于 $-\alpha$};

% 平滑过渡标注
\addplot[mark=*, blue] coordinates {(0,0)};
\node[blue, above right] at (axis cs:0,0.2) {平滑过渡};
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- 负区域平滑过渡到-α
- 比ReLU更鲁棒（对噪声）

#### 优点
✅ **均值接近0**：加速收敛  
✅ **负区域平滑**：不会神经元死亡  
✅ **鲁棒性强**：对噪声输入更稳定

#### 缺点
❌ **计算开销大**：负区域有指数运算  
❌ 超参数α需要调整

#### 适用场景
- ✅ 深度神经网络（追求更好性能时）
- ✅ 对噪声敏感的任务

---

### 6. GELU（高斯误差线性单元）

#### 公式
$$\text{GELU}(z) = z \cdot \Phi(z)$$

其中 $\Phi(z)$ 是标准正态分布的累积分布函数（CDF）：
$$\Phi(z) = \frac{1}{2}\left[1 + \text{erf}\left(\frac{z}{\sqrt{2}}\right)\right]$$

**近似公式**（更常用）：
$$\text{GELU}(z) \approx 0.5z\left(1 + \tanh\left[\sqrt{\frac{2}{\pi}}\left(z + 0.044715z^3\right)\right]\right)$$

#### 导数
$$\text{GELU}'(z) = \Phi(z) + z \cdot \phi(z)$$

其中 $\phi(z) = \frac{1}{\sqrt{2\pi}}e^{-z^2/2}$ 是标准正态分布的概率密度函数（PDF）

#### 特性
- **输出范围**：(-0.17z, +∞)（大致）
- **平滑性**：比ReLU更平滑
- **概率解释**：根据输入的高斯分布概率决定激活程度

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$f(z)$},
    title={GELU vs ReLU},
    grid=major,
    domain=-3:3,
    samples=200,
    ymin=-0.5, ymax=3.5,
    xmin=-3, xmax=3,
    legend pos=north west,
    axis lines=middle,
]
% GELU approximation
\addplot[purple, thick, smooth] {0.5*x*(1 + tanh(sqrt(2/pi)*(x + 0.044715*x^3)))};
\addlegendentry{GELU}

% ReLU对比
\addplot[gray, dashed] {max(0,x)};
\addlegendentry{ReLU}

% 平滑过渡区域
\fill[purple!10, opacity=0.5] (axis cs:-1,-0.2) rectangle (axis cs:1,0.8);
\node[purple] at (axis cs:0,1.5) {平滑过渡区};
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- 负区域不为0，但接近0
- 在0附近平滑过渡
- 类似"概率门控"的ReLU

#### 优点
✅ **平滑非线性**：在0附近更平滑  
✅ **避免神经元死亡**：负区域仍有小激活  
✅ **Transformer标配**：BERT、GPT等模型默认激活函数

#### 缺点
❌ **计算复杂**：涉及误差函数或Tanh近似  
❌ 可解释性稍弱

#### 适用场景
- ✅ **Transformer模型**（BERT、GPT）
- ✅ **NLP任务**
- ✅ 需要平滑激活的深度网络

---

### 7. Swish（自门控激活函数）

#### 公式
$$\text{Swish}(z) = z \cdot \sigma(z) = \frac{z}{1 + e^{-z}}$$

或参数化版本：
$$\text{Swish}(z; \beta) = z \cdot \sigma(\beta z)$$

当 $\beta = 1$ 时为标准Swish

#### 导数
$$\text{Swish}'(z) = \sigma(z) + z \cdot \sigma(z)(1 - \sigma(z)) = \sigma(z) + z \cdot \sigma'(z)$$

#### 特性
- **输出范围**：(-0.28z, +∞)（大致）
- **自门控**：输入自己调制自己
- **平滑性**：无处不可微

#### 函数曲线

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=7cm,
    xlabel={$z$}, ylabel={$f(z)$},
    title={Swish vs ReLU},
    grid=major,
    domain=-3:3,
    samples=100,
    ymin=-0.5, ymax=3.5,
    xmin=-3, xmax=3,
    legend pos=north west,
    axis lines=middle,
]
% Swish
\addplot[cyan!70!blue, thick, smooth] {x/(1+exp(-x))};
\addlegendentry{Swish}

% ReLU对比
\addplot[gray, dashed] {max(0,x)};
\addlegendentry{ReLU}

% 标注最小值点
\addplot[mark=*, red] coordinates {(-0.3,-0.083)};
\node[red, below] at (axis cs:-0.3,-0.2) {最小值};

% 平滑特性标注
\node[cyan!70!blue] at (axis cs:1.5,2.5) {平滑、自门控};
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- 负区域有小负值（约-0.28z在最小处）
- 随z增大逐渐趋向线性
- 类似GELU但更简单

#### 优点
✅ **平滑无界**：比ReLU更平滑  
✅ **自门控机制**：隐式学习门控  
✅ **性能优异**：在图像分类等任务上超越ReLU

#### 缺点
❌ **计算稍复杂**：包含Sigmoid  
❌ 训练初期可能不稳定

#### 适用场景
- ✅ **深度CNN**（如EfficientNet）
- ✅ 图像分类任务
- ✅ 替代ReLU追求更好性能

---

### 8. Softmax（多分类输出层）

#### 公式
$$\text{Softmax}(z_i) = \frac{e^{z_i}}{\sum_{j=1}^{K} e^{z_j}}$$

其中 $z = [z_1, z_2, \ldots, z_K]$ 是K类的logits

#### 导数
$$\frac{\partial \text{Softmax}(z_i)}{\partial z_j} = \begin{cases} \text{Softmax}(z_i)(1 - \text{Softmax}(z_i)) & \text{if } i = j \\ -\text{Softmax}(z_i) \cdot \text{Softmax}(z_j) & \text{if } i \neq j \end{cases}$$

#### 特性
- **输出范围**：(0, 1)
- **归一化**：$\sum_{i=1}^{K} \text{Softmax}(z_i) = 1$
- **概率解释**：输出可视为类别概率

#### 可视化（3类示例）

```latex {.tikz}
\begin{tikzpicture}
\begin{axis}[
    width=10cm, height=6cm,
    xlabel={logit值},
    ylabel={概率},
    title={Softmax函数示例（3类）},
    grid=major,
    domain=0:10,
    samples=50,
    ymin=0, ymax=1,
    xmin=0, xmax=10,
    legend pos=outer north east,
    axis lines=left,
]
% 模拟三个类别的Softmax输出
% 假设 z1=x, z2=5, z3=3
\addplot[blue, thick] {exp(x)/(exp(x)+exp(5)+exp(3))};
\addlegendentry{类别1}

\addplot[red, thick] {exp(5)/(exp(x)+exp(5)+exp(3))};
\addlegendentry{类别2}

\addplot[green!70!black, thick] {exp(3)/(exp(x)+exp(5)+exp(3))};
\addlegendentry{类别3}

% 标注竞争关系
\node at (axis cs:5,0.7) {概率分配竞争};
\draw[->] (axis cs:5,0.65) -- (axis cs:3,0.6);
\draw[->] (axis cs:5,0.65) -- (axis cs:7,0.5);
\end{axis}
\end{tikzpicture}
```

**关键特征：**
- 所有输出之和为1（概率分布）
- 最大logit对应最大概率
- 类别间相互竞争

#### 优点
✅ **概率输出**：可解释为类别概率  
✅ **多分类标配**：配合交叉熵损失  
✅ **数值稳定技巧**：减去max(z)防止溢出

#### 缺点
❌ **计算开销**：需要计算所有类别的指数  
❌ **梯度消失**：饱和时梯度小

#### 适用场景
- ✅ **多分类输出层**（必备）
- ✅ 注意力机制（Attention）
- ⚠️ 不用于隐藏层

---

## 激活函数对比总结

| 激活函数 | 输出范围 | 零中心 | 计算复杂度 | 梯度消失 | 主要应用 |
|----------|----------|--------|------------|----------|----------|
| **Sigmoid** | (0, 1) | ❌ | 高 | ✅ 严重 | 二分类输出层 |
| **Tanh** | (-1, 1) | ✅ | 高 | ✅ 存在 | RNN/LSTM隐藏层 |
| **ReLU** | [0, +∞) | ❌ | 低 | ❌ 缓解 | 深度网络隐藏层（最常用） |
| **Leaky ReLU** | (-∞, +∞) | ❌ | 低 | ❌ 缓解 | 替代ReLU |
| **ELU** | (-α, +∞) | 接近 | 中 | ❌ 缓解 | 深度网络（追求性能） |
| **GELU** | 约(-0.17z, +∞) | 接近 | 高 | ❌ 缓解 | Transformer/NLP |
| **Swish** | 约(-0.28z, +∞) | ❌ | 中 | ❌ 缓解 | 深度CNN |
| **Softmax** | (0, 1) | ❌ | 高 | ✅ 存在 | 多分类输出层 |

---

## 如何选择激活函数？

### 1. 隐藏层
- **首选 ReLU**：简单高效，适合大多数场景
- **ReLU失效时**：尝试 Leaky ReLU、ELU
- **追求极致性能**：GELU（Transformer）、Swish（CNN）
- **RNN/LSTM**：Tanh（配合Sigmoid门控）

### 2. 输出层
- **二分类**：Sigmoid
- **多分类**：Softmax
- **回归任务**：线性（或ReLU保证非负）

### 3. 调优建议
1. **从ReLU开始**：快速验证模型架构
2. **监控神经元死亡**：大量0输出时切换到Leaky ReLU
3. **跟随SOTA模型**：
   - Transformer → GELU
   - ResNet/EfficientNet → ReLU/Swish
   - LSTM → Tanh
4. **注意初始化**：配合He初始化（ReLU系）或Xavier初始化（Tanh/Sigmoid）

---

## 附录：TikZ绘图使用指南

### 环境配置

**LaTeX包需求：**
```latex
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
```

**VS Code插件：**
- [Markdown Preview Enhanced](https://marketplace.visualstudio.com/items?itemName=shd101wyy.markdown-preview-enhanced)

**LaTeX发行版（二选一）：**
- [MiKTeX](https://miktex.org/) - Windows推荐
- [TeX Live](https://www.tug.org/texlive/) - 跨平台

### 基本语法

**代码块标记：**
````markdown
```latex {.tikz}
\begin{tikzpicture}
  % TikZ代码
\end{tikzpicture}
```
````

**函数绘制：**
```latex
\addplot[颜色, 样式] {表达式};
```

**常用表达式：**
- `1/(1+exp(-x))` - Sigmoid
- `tanh(x)` - Tanh
- `max(0,x)` - ReLU
- `x>0 ? x : 0.01*x` - Leaky ReLU

### 在线测试

如果本地无法渲染，可以：
1. 复制LaTeX代码到 [Overleaf](https://www.overleaf.com/)
2. 创建新项目，选择 "Blank Project"
3. 在main.tex中粘贴代码（记得添加必要的包）
4. 点击 "Recompile" 查看效果

---

**文档版本**：v1.0  
**最后更新**：2024年  
**作者**：AI助手整理
